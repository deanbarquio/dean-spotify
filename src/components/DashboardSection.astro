---
// DashboardSection.astro — Spotify Home Dashboard
const madeForYou = [
  {
    mixNum: '1',
    title: 'Daily Mix 1',
    sub: 'React, NestJS, Firebase, Google Cloud',
    accent: '#FFB6C1', // Light pink
    bgColor: '#f9cbd3',
    waveColor: '#ff6b8b',
    iconColor: '#d64c6d'
  },
  {
    mixNum: '2',
    title: 'Daily Mix 2',
    sub: 'Next.js, Elasticsearch, Docker, TailwindCSS',
    accent: '#B0E0E6', // Light blue
    bgColor: '#b8ebd0',
    waveColor: '#4bc383',
    iconColor: '#2b965c'
  },
  {
    mixNum: '3',
    title: 'Daily Mix 3',
    sub: 'Angular, Svelte, TypeScript, Verdaccio',
    accent: '#E6E6FA', // Lavender
    bgColor: '#e3beeb',
    waveColor: '#aa55d4',
    iconColor: '#7b2e9e'
  },
  {
    mixNum: '4',
    title: 'Daily Mix 4',
    sub: 'Prisma, MySQL, PostgreSQL, TanStack Query',
    accent: '#FFE4E1', // Misty Rose
    bgColor: '#ffdead',
    waveColor: '#ffa07a',
    iconColor: '#cd853f'
  },
  {
    mixNum: '5',
    title: 'Daily Mix 5',
    sub: 'Flutter, Kotlin, MapLibre, OpenStreetMap',
    accent: '#FFF0F5', // Lavender Blush
    bgColor: '#ffd1a9',
    waveColor: '#ff7f50',
    iconColor: '#d2691e'
  },
];

const recentWork = [
  { name: 'Manpower Costing Module', project: 'Constrack', plays: '315k', accent: '#F6A800' },
  { name: 'Search & Filter Engine', project: 'Control Panel', plays: '215k', accent: '#3B82F6' },
  { name: 'Live GPS Tracker', project: 'Hikemate', plays: '180k', accent: '#F97316' },
  { name: 'Data Table Component', project: 'Component Pantry', plays: '142k', accent: '#8B5CF6' },
];

const featuredBuild = recentWork[0];

const topSkillCards = [
  {
    title: 'Angular',
    eyebrow: 'Framework',
    desc: 'Build enterprise-grade SPAs with TypeScript-first architecture and reactive signals.',
    accent: '#DD0031',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20" style="color:#DD0031"><path d="M12 2L2 5.5l1.5 13.5 8.5 5 8.5-5 1.5-13.5L12 2zm0 3.2L19.3 16H16l-1.5-3.8h-5L8 16H4.7L12 5.2zm-1.8 5.4h3.6L12 7.7l-1.8 2.9z"/></svg>`,
  },
  {
    title: 'React / Next.js',
    eyebrow: 'UI Library',
    desc: 'Compose dynamic interfaces with component-driven patterns and server rendering.',
    accent: '#61DAFB',
    icon: `<svg viewBox="-11.5 -10.23 23 20.46" fill="none" stroke="#61DAFB" stroke-width="1.2" width="20" height="20"><circle cx="0" cy="0" r="2.05" fill="#61DAFB"/><ellipse rx="11" ry="4.2"/><ellipse rx="11" ry="4.2" transform="rotate(60)"/><ellipse rx="11" ry="4.2" transform="rotate(120)"/></svg>`,
  },
  {
    title: 'NestJS',
    eyebrow: 'Backend',
    desc: 'Architect scalable server-side apps with decorators, modules, and dependency injection.',
    accent: '#E0234E',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20" style="color:#E0234E"><path d="M12.086 2.457L3.02 7.697v10.483l9.066 5.234 9.068-5.234V7.697l-9.068-5.24zm0 2.309l6.732 3.89-6.732 3.886-6.73-3.886 6.73-3.89zm-6.73 6.199l6.73 3.886v7.778l-6.73-3.89v-7.774zm13.462 0v7.774l-6.73 3.89v-7.778l6.73-3.886z"/></svg>`,
  },
];

const otherSkillCards = [
  {
    title: 'TailwindCSS',
    eyebrow: 'Styling',
    desc: 'Utility-first CSS for rapid, consistent UI development.',
    accent: '#38BDF8',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20" style="color:#38BDF8"><path d="M12.001 4.8c-3.2 0-5.2 1.6-6 4.8 1.2-1.6 2.6-2.2 4.2-1.8.913.228 1.565.89 2.288 1.624C13.666 10.618 15.027 12 18.001 12c3.2 0 5.2-1.6 6-4.8-1.2 1.6-2.6 2.2-4.2 1.8-.913-.228-1.565-.89-2.288-1.624C16.337 6.182 14.976 4.8 12.001 4.8zm-6 7.2c-3.2 0-5.2 1.6-6 4.8 1.2-1.6 2.6-2.2 4.2-1.8.913.228 1.565.89 2.288 1.624C7.666 17.818 9.027 19 12.001 19c3.2 0 5.2-1.6 6-4.8-1.2 1.6-2.6 2.2-4.2 1.8-.913-.228-1.565-.89-2.288-1.624C10.337 13.382 8.976 12 6.001 12z"/></svg>`,
  },
  {
    title: 'Docker',
    eyebrow: 'DevOps',
    desc: 'Containerize and deploy reproducible environments.',
    accent: '#2496ED',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20" style="color:#2496ED"><path d="M13.983 11.078h2.119a.186.186 0 00.186-.186V8.773a.186.186 0 00-.186-.186h-2.119a.186.186 0 00-.186.186v2.119c0 .102.084.186.186.186zM4.17 15.248c0 1.075.852 1.954 1.925 1.954h6.059c2.193 0 4.148-1.547 4.544-3.702.137-.745.022-1.492-.288-2.16a1.8 1.8 0 00-1.222-1.047l-.234-.092c-.868-.344-1.896-.282-2.738.163a1.2 1.2 0 00-.326.23l-.15.15v-.425c0-1.077-.852-1.956-1.925-1.956h-.7v2.119c0 .546-.356.966-.88 1.053-.473.084-.962-.127-1.18-.542-.1-.19-.153-.404-.153-.623V7.933a.186.186 0 00-.186-.186H4.357a.186.186 0 00-.186.186v7.315z"/></svg>`,
  },
];
---

<section id="dashboard" style="padding-bottom:40px;">

  <div style="padding:0 32px;">

    <!-- ① NATIVE SPOTIFY GREETING HEADER -->
    <h2 id="greeting-text" style="font-size:1.8rem;font-weight:800;color:#fff;letter-spacing:-0.02em;margin-bottom:8px;padding-top:24px;">Good evening</h2>

    <!-- ② QUICK ACCESS PILLS -->
    <div style="display:flex;flex-wrap:wrap;gap:8px;padding:24px 0 8px;">
      {['All', 'Music', 'Projects', 'Skills', 'Podcasts'].map((t, i) => (
        <button class="pill-btn" data-active={i===0}
          style={`padding:7px 16px;border-radius:500px;border:none;cursor:pointer;font-size:0.78rem;font-weight:700;transition:background 0.15s,color 0.15s;background:${i===0?'#fff':'rgba(255,255,255,0.1)'};color:${i===0?'#000':'#fff'};`}
          onclick="document.querySelectorAll('.pill-btn').forEach(b=>{b.style.background='rgba(255,255,255,0.1)';b.style.color='#fff'});this.style.background='#fff';this.style.color='#000'">
          {t}
        </button>
      ))}
    </div>

    <!-- ③ GOOD EVENING QUICK LINKS (2x3 grid) -->
    <div style="margin:24px 0;">
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px;" class="quick-grid">
        {[
          { title:'About Me', sub:'Software Engineer · Cebu', href:'#about', accent:'#1DB954' },
          { title:'Constrack', sub:'Full-Stack · React+NestJS', href:'#projects', accent:'#F6A800' },
          { title:'Control Panel', sub:'Next.js · Elasticsearch', href:'#projects', accent:'#3B82F6' },
          { title:'Experience', sub:'1yr+ at N-Compass TV', href:'#experience', accent:'#8B5CF6' },
          { title:'Skills', sub:'Angular · React · NestJS', href:'#skills', accent:'#F97316' },
          { title:'Contact', sub:'Open to opportunities', href:'#contact', accent:'#10B981' },
        ].map(item => (
          <a href={item.href}
            style="display:flex;align-items:center;gap:0;background:var(--sp-elevated);border-radius:6px;overflow:hidden;text-decoration:none;transition:background 0.15s,transform 0.1s;border:1px solid rgba(255,255,255,0.04);"
            onmouseover="this.style.background='rgba(255,255,255,0.14)';this.style.transform='translateY(-1px)'"
            onmouseout="this.style.background='var(--sp-elevated)';this.style.transform='translateY(0)'">
            <div style={`width:56px;height:56px;background:${item.accent};display:flex;align-items:center;justify-content:center;flex-shrink:0;`}>
              <div style="width:20px;height:20px;border-radius:50%;background:rgba(0,0,0,0.3);"></div>
            </div>
            <div style="padding:10px 14px;flex:1;min-width:0;">
              <p style="font-size:0.8rem;font-weight:700;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{item.title}</p>
              <p style="font-size:0.65rem;color:var(--sp-text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{item.sub}</p>
            </div>
          </a>
        ))}
      </div>
    </div>

    <!-- ④ MADE FOR YOU — horizontal scroll -->
    <div style="margin-bottom:40px;">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">
        <h2 style="font-size:1.3rem;font-weight:800;color:#fff;letter-spacing:-0.02em;">Made For You</h2>
        <a href="#projects" style="font-size:0.72rem;font-weight:700;color:var(--sp-text);text-decoration:none;text-transform:uppercase;letter-spacing:0.08em;transition:color 0.15s;"
          onmouseover="this.style.color='#fff'" onmouseout="this.style.color='var(--sp-text)'">Show all</a>
      </div>
      <div style="display:grid;grid-template-columns:repeat(5,1fr);gap:16px;" class="mfy-grid">
        {madeForYou.map(card => (
          <div class="sp-card" onclick="document.getElementById('projects').scrollIntoView({behavior:'smooth'})"
            style="cursor:pointer;background:#181818;padding:16px;border-radius:8px;transition:background 0.2s;"
            onmouseover="this.style.background='#282828'"
            onmouseout="this.style.background='#181818'">
            <!-- Square art precisely matching Spotify Daily Mix -->
            <div style={`width:100%;aspect-ratio:1/1;border-radius:6px;background:${card.bgColor};position:relative;overflow:hidden;margin-bottom:12px;box-shadow:0 8px 24px rgba(0,0,0,0.4);`}
              class="sp-card-art">
              
              <!-- Wave shapes mimicking the wavy design in Daily Mixes -->
              <div style={`position:absolute;bottom:0;left:0;right:0;height:70%;background:${card.waveColor};clip-path:ellipse(90% 60% at 30% 100%);`}></div>
              <div style={`position:absolute;bottom:0;left:-20%;right:-20%;height:45%;background:#fff;clip-path:ellipse(80% 50% at 60% 100%);`}></div>
              
              <!-- Spotify tiny logo top-left -->
              <div style="position:absolute;top:10px;left:10px;display:flex;align-items:center;gap:4px;z-index:2;opacity:0.9;">
                <svg viewBox="0 0 24 24" fill="#000" width="16" height="16"><path d="M12 2C6.477 2 2 6.477 2 12s4.477 10 10 10 10-4.477 10-10S17.523 2 12 2zm4.586 14.424c-.18.295-.565.387-.86.207-2.377-1.454-5.37-1.783-8.893-1.022-.336.073-.668-.142-.74-.477-.074-.335.14-.667.476-.74 3.845-.878 7.14-.505 9.81 1.13.295.18.387.564.207.862zm1.226-2.724c-.226.367-.707.487-1.074.26-2.72-1.672-6.87-2.157-10.076-1.183-.412.125-.845-.107-.97-.52-.126-.412.107-.845.52-.97 3.668-1.112 8.243-.574 11.34 1.33.367.227.487.708.26 1.075zm.106-2.834C14.39 8.718 8.563 8.525 5.176 9.553c-.52.158-1.07-.14-1.227-.66-.158-.52.14-1.07.66-1.228 3.896-1.182 10.334-.96 14.385 1.447.47.28.623.89.344 1.36-.28.47-.89.622-1.36.343z"/></svg>
              </div>

              <!-- Text block on cover matching screenshot exactly -->
              <div style="position:absolute;bottom:14px;left:0;right:0;text-align:center;z-index:3;pointer-events:none;">
                <p style="font-family:'Circular Std',sans-serif;font-size:1.05rem;font-weight:900;color:#000;letter-spacing:-0.03em;margin:0;text-transform:uppercase;">Daily Mix</p>
                <p style={`font-family:'Circular Std',sans-serif;font-size:3.2rem;font-weight:900;color:${card.iconColor};line-height:0.8;margin:-2px 0 0;`}>{card.mixNum}</p>
              </div>

              <!-- Play btn overlay -->
              <div class="sp-play-btn" style="position:absolute;bottom:8px;right:8px;width:40px;height:40px;background:var(--sp-green);border-radius:50%;display:flex;align-items:center;justify-content:center;opacity:0;transform:translateY(6px);transition:opacity 0.2s,transform 0.2s;box-shadow:0 8px 24px rgba(0,0,0,0.5);z-index:4;">
                <svg viewBox="0 0 24 24" fill="black" width="16" height="16"><path d="M8 5v14l11-7z"/></svg>
              </div>
            </div>
            <p style="font-size:0.84rem;font-weight:700;color:#fff;margin-bottom:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{card.title}</p>
            <p style="font-size:0.72rem;color:var(--sp-text);line-height:1.3;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;text-overflow:ellipsis;">{card.sub}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- ⑤ SPLIT ROW: "What You Work With" + "Now Playing" hero -->
    <div class="split-grid dash-split">

      <!-- Left: What You Work With -->
      <div class="wyw-panel">
        <div class="wyw-header">
          <h3 class="wyw-title">What You Work With</h3>
          <a href="#skills" class="wyw-link">see more</a>
        </div>

        <div class="wyw-grid-top">
          {topSkillCards.map(s => (
            <a href="#skills" class="mono-card" style={`--mono-accent:${s.accent};`}>
              <div class="mono-card__noise" aria-hidden="true"></div>
              <div class="mono-card__glow" aria-hidden="true"></div>
              <div class="mono-card__badge">
                <span set:html={s.icon} />
              </div>
              <div class="mono-card__body">
                <h4 class="mono-card__title">{s.title}</h4>
                <p class="mono-card__desc">{s.desc}</p>
              </div>
              <div class="mono-card__footer">
                <span class="mono-card__tag">{s.eyebrow}</span>
                <span class="mono-card__more">View more ▾</span>
              </div>
            </a>
          ))}
        </div>

        <div class="wyw-grid-bottom">
          {otherSkillCards.map(s => (
            <a href="#skills" class="mono-card mono-card--wide" style={`--mono-accent:${s.accent};`}>
              <div class="mono-card__noise" aria-hidden="true"></div>
              <div class="mono-card__glow" aria-hidden="true"></div>
              <div class="mono-card__badge">
                <span set:html={s.icon} />
              </div>
              <div class="mono-card__body">
                <h4 class="mono-card__title">{s.title}</h4>
                <p class="mono-card__desc">{s.desc}</p>
              </div>
              <div class="mono-card__footer">
                <span class="mono-card__tag">{s.eyebrow}</span>
                <span class="mono-card__more">View more ▾</span>
              </div>
            </a>
          ))}
        </div>
      </div>

      <!-- Right: Now Playing — golden hero card + track list -->
      <div class="np-column">

        <div class="np-tracks-panel">
          <div class="wyw-header np-tracks-header">
            <h3 class="wyw-title">Track of the Week</h3>
            <a href="#projects" class="wyw-link">see more</a>
          </div>
          <div class="np-tracks-list">
            {recentWork.map((item, i) => (
              <a href="#projects" class="np-track-row" style={`--track-accent:${item.accent};`}>
                <span class="np-track-border" aria-hidden="true"></span>
                <span class="np-track-rank">{String(i + 1).padStart(2, '0')}</span>
                <div class="np-track-icon">
                  <span class="np-track-dot"></span>
                </div>
                <div class="np-track-meta">
                  <p class="np-track-name">{item.name}</p>
                  <p class="np-track-project">{item.project}</p>
                </div>
                <span class="np-track-plays">{item.plays}</span>
                <span class="np-track-play" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="black" width="10" height="10"><path d="M8 5v14l11-7z"/></svg>
                </span>
              </a>
            ))}
          </div>
        </div>
      </div>
    </div>

    <!-- ⑥ ABOUT — Artist profile card -->
    <div id="about" style="margin-bottom:8px;">
      <h2 style="font-size:1.3rem;font-weight:800;color:#fff;letter-spacing:-0.02em;margin-bottom:16px;">About the Artist</h2>
      <div style="background:var(--sp-surface);border-radius:12px;overflow:hidden;display:grid;grid-template-columns:1fr 1fr;" class="about-grid">

        <!-- Left text -->
        <div style="padding:32px;position:relative;overflow:hidden;">
          <div style="position:absolute;top:-40px;right:-40px;width:180px;height:180px;background:radial-gradient(circle,rgba(29,185,84,0.08),transparent 70%);pointer-events:none;"></div>
          <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px;">
            <div style="width:72px;height:72px;border-radius:50%;background:linear-gradient(135deg,#1DB954,#0a4a25);display:flex;align-items:center;justify-content:center;font-size:1.4rem;font-weight:800;color:#000;border:2px solid rgba(29,185,84,0.4);flex-shrink:0;">DB</div>
            <div>
              <p style="font-size:1rem;font-weight:800;color:#fff;letter-spacing:-0.02em;">Dean Lourence P. Barquio</p>
              <p style="font-size:0.75rem;color:var(--sp-green);font-weight:600;margin-top:2px;">Software Engineer · Frontend Developer</p>
              <p style="font-size:0.68rem;color:var(--sp-text-dim);margin-top:3px;">📍 Consolacion, Cebu · 🎓 Magna Cum Laude</p>
            </div>
          </div>
          <p style="font-size:0.84rem;color:var(--sp-text);line-height:1.75;margin-bottom:14px;">
            Full-Stack Developer specializing in <strong style="color:#fff;">Angular, React & NestJS</strong>. Went from QA intern → Full-Stack Developer → Software Engineer at the same company in under 6 months.
          </p>
          <p style="font-size:0.82rem;color:var(--sp-text);line-height:1.75;margin-bottom:20px;">
            I ship scalable frontends with AI-assisted workflows using Claude, Synk, and Cursor. Available for full-time, freelance, or contract roles.
          </p>
          <div style="display:flex;gap:10px;flex-wrap:wrap;">
            <a href="mailto:deanbarquio@gmail.com" class="btn-primary" style="text-decoration:none;font-size:0.76rem;padding:9px 22px;">
              <span style="display:flex;align-items:center;gap:6px;">
                <svg viewBox="0 0 24 24" fill="currentColor" width="14" height="14"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
                Hire Me
              </span>
            </a>
            <a href="https://github.com/deanbarquio" target="_blank" rel="noopener" class="btn-outline" style="text-decoration:none;font-size:0.76rem;padding:9px 22px;">GitHub</a>
          </div>
        </div>

        <!-- Right stats -->
        <div style="padding:32px;background:rgba(255,255,255,0.02);border-left:1px solid var(--sp-border);display:flex;flex-direction:column;gap:12px;">
          <p style="font-size:0.68rem;font-weight:700;color:var(--sp-text-dim);text-transform:uppercase;letter-spacing:0.1em;margin-bottom:4px;">Career Stats</p>
          {[
            { value:'5+', label:'Projects Shipped', accent:'#1DB954' },
            { value:'1yr+', label:'Professional Experience', accent:'#F6A800' },
            { value:'10+', label:'Technologies Mastered', accent:'#3B82F6' },
            { value:'3', label:'Roles at N-Compass TV', accent:'#8B5CF6' },
          ].map(s => (
            <div style="display:flex;align-items:center;gap:14px;padding:12px 16px;background:var(--sp-elevated);border-radius:8px;">
              <p style={`font-size:1.6rem;font-weight:800;color:${s.accent};letter-spacing:-0.03em;line-height:1;min-width:52px;`}>{s.value}</p>
              <p style="font-size:0.78rem;color:var(--sp-text);">{s.label}</p>
            </div>
          ))}
          <div style="display:flex;align-items:center;gap:8px;padding:10px 16px;background:rgba(29,185,84,0.08);border-radius:8px;border:1px solid rgba(29,185,84,0.2);margin-top:4px;">
            <span style="width:8px;height:8px;border-radius:50%;background:var(--sp-green);box-shadow:0 0 8px var(--sp-green);animation:sp-pulse 2s ease infinite;flex-shrink:0;"></span>
            <p style="font-size:0.78rem;font-weight:600;color:#fff;">Available for new opportunities</p>
          </div>
        </div>
      </div>
    </div>

  </div><!-- /padding wrapper -->
</section>

<style>
  @keyframes sp-pulse {
    0%,100%{opacity:1;transform:scale(1)}
    50%{opacity:.5;transform:scale(1.4)}
  }
  .sp-card:hover .sp-play-btn { opacity:1!important;transform:translateY(0)!important; }
  .sp-card:hover .sp-card-art { box-shadow:0 12px 40px rgba(0,0,0,0.6)!important; }

  /* ── Split row: skills + Now Playing ── */
  .dash-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-bottom: 40px;
    align-items: stretch;
  }

  .wyw-panel,
  .np-column {
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.04) 0%, rgba(24, 24, 24, 0.6) 100%);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-radius: 20px;
    padding: 24px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow:
      0 0 0 1px rgba(0, 0, 0, 0.4),
      0 -24px 80px -24px rgba(134, 134, 240, 0.08) inset,
      0 24px 48px -12px rgba(0, 0, 0, 0.5);
  }

  .wyw-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 18px;
  }

  .wyw-title {
    font-size: 1rem;
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.02em;
  }

  .wyw-link {
    font-size: 0.7rem;
    color: var(--sp-text);
    text-decoration: none;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    transition: color 0.15s;
  }
  .wyw-link:hover { color: #fff; }

  .wyw-grid-top {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }

  .wyw-grid-bottom {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 12px;
  }

  /* ── Monochrome skill cards ── */
  .mono-card {
    position: relative;
    display: flex;
    flex-direction: column;
    text-decoration: none;
    border-radius: 20px;
    padding: 22px;
    background: linear-gradient(145deg, #1e1e1e 0%, #141414 50%, #0e0e0e 100%);
    border: 1px solid rgba(255, 255, 255, 0.07);
    overflow: hidden;
    isolation: isolate;
    min-height: 210px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.05),
      0 8px 32px rgba(0, 0, 0, 0.4);
    transition:
      transform 0.4s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.4s ease,
      border-color 0.4s ease;
  }
  .mono-card:hover {
    transform: translateY(-6px);
    border-color: color-mix(in srgb, var(--mono-accent) 40%, rgba(255,255,255,0.12));
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.08),
      0 24px 48px -8px rgba(0, 0, 0, 0.6),
      0 0 0 1px color-mix(in srgb, var(--mono-accent) 20%, transparent);
  }

  .mono-card__noise {
    position: absolute;
    inset: 0;
    opacity: 0.035;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1;
  }

  .mono-card__glow {
    position: absolute;
    top: -40%;
    right: -30%;
    width: 160px;
    height: 160px;
    border-radius: 50%;
    background: radial-gradient(circle, color-mix(in srgb, var(--mono-accent) 25%, transparent) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
    opacity: 0.5;
    transition: opacity 0.4s, transform 0.4s;
  }
  .mono-card:hover .mono-card__glow {
    opacity: 1;
    transform: scale(1.3);
  }

  .mono-card__badge {
    position: absolute;
    top: 18px;
    right: 18px;
    width: 44px;
    height: 44px;
    border-radius: 14px;
    background: linear-gradient(145deg, rgba(255,255,255,0.08), rgba(0,0,0,0.3));
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 3;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.1),
      0 4px 16px rgba(0,0,0,0.3);
    transition: transform 0.35s cubic-bezier(0.22,1,0.36,1), box-shadow 0.35s;
  }
  .mono-card:hover .mono-card__badge {
    transform: scale(1.08) rotate(-3deg);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.15),
      0 0 24px color-mix(in srgb, var(--mono-accent) 40%, transparent),
      0 6px 20px rgba(0,0,0,0.35);
  }

  .mono-card__body {
    position: relative;
    z-index: 2;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-right: 52px;
    gap: 8px;
  }

  .mono-card__title {
    font-size: 1.05rem;
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .mono-card__desc {
    font-size: 0.72rem;
    color: var(--sp-text);
    line-height: 1.55;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .mono-card__footer {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 14px;
    padding-top: 12px;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }

  .mono-card__tag {
    font-size: 0.6rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: color-mix(in srgb, var(--mono-accent) 85%, #fff);
    padding: 3px 10px;
    border-radius: 500px;
    background: color-mix(in srgb, var(--mono-accent) 12%, transparent);
    border: 1px solid color-mix(in srgb, var(--mono-accent) 20%, transparent);
  }

  .mono-card__more {
    font-size: 0.65rem;
    font-weight: 600;
    color: var(--sp-text-dim);
    letter-spacing: 0.02em;
    transition: color 0.25s;
  }
  .mono-card:hover .mono-card__more {
    color: var(--mono-accent);
  }

  .mono-card--wide {
    min-height: 140px;
  }

  /* Now Playing hero */
  .np-column {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 18px;
  }

  .np-hero {
    position: relative;
    display: block;
    min-height: 260px;
    flex: 1;
    border-radius: 24px;
    overflow: hidden;
    text-decoration: none;
    background: linear-gradient(135deg, #ffe566 0%, #f5c842 28%, #d4a82a 58%, #9a7b1a 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.45),
      0 20px 50px -12px rgba(0, 0, 0, 0.5);
    transition: transform 0.4s cubic-bezier(0.22, 1, 0.36, 1), box-shadow 0.4s ease;
    isolation: isolate;
  }
  .np-hero:hover {
    transform: translateY(-2px) scale(1.01);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.5),
      0 28px 56px -10px rgba(0, 0, 0, 0.55);
  }

  .np-hero-ring {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    padding: 1px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.5), transparent 40%, rgba(0, 0, 0, 0.15));
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    pointer-events: none;
    z-index: 4;
  }

  .np-hero-noise {
    position: absolute;
    inset: 0;
    opacity: 0.04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1;
  }

  .np-hero-shine {
    position: absolute;
    inset: 0;
    background: linear-gradient(125deg, rgba(255, 255, 255, 0.45) 0%, transparent 42%);
    pointer-events: none;
  }

  .np-hero-vignette {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 20% 90%, rgba(0, 0, 0, 0.12) 0%, transparent 55%),
      radial-gradient(ellipse at 100% 0%, rgba(255, 255, 255, 0.15) 0%, transparent 40%);
    pointer-events: none;
  }

  .np-hero-copy {
    position: absolute;
    left: 0;
    bottom: 0;
    z-index: 2;
    padding: 22px 24px;
    max-width: 48%;
  }

  .np-hero-title {
    font-size: 1.35rem;
    font-weight: 800;
    color: #141414;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 6px;
  }

  .np-hero-track {
    font-size: 0.82rem;
    font-weight: 500;
    color: rgba(20, 20, 20, 0.62);
    line-height: 1.4;
  }

  .np-hero-artist {
    font-weight: 700;
    color: rgba(20, 20, 20, 0.78);
  }

  .np-hero-sep {
    margin: 0 4px;
    opacity: 0.5;
  }

  .np-stack-art {
    position: absolute;
    right: -20px;
    top: 50%;
    transform: translateY(-50%);
    width: min(78%, 380px);
    height: auto;
    z-index: 1;
    pointer-events: none;
    animation: stack-float 5s ease-in-out infinite;
  }

  .np-stack-svg {
    width: 100%;
    height: auto;
    display: block;
  }

  @keyframes stack-float {
    0%, 100% { transform: translateY(-50%); }
    50% { transform: translateY(calc(-50% - 10px)); }
  }

  .np-play-btn {
    position: absolute;
    right: 22px;
    bottom: 22px;
    z-index: 3;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: #fff;
    border: 1px solid rgba(0, 0, 0, 0.06);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow:
      0 8px 24px rgba(0, 0, 0, 0.22),
      0 2px 6px rgba(0, 0, 0, 0.12);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .np-hero:hover .np-play-btn {
    transform: scale(1.08);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.28);
  }

  .np-tracks-panel {
    background: rgba(0, 0, 0, 0.35);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-radius: 16px;
    padding: 16px;
    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .np-tracks-header { margin-bottom: 12px; }

  .np-tracks-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .np-track-row {
    position: relative;
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px 12px;
    border-radius: 12px;
    text-decoration: none;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.03) 0%, rgba(255, 255, 255, 0.01) 100%);
    border: 1px solid rgba(255, 255, 255, 0.05);
    overflow: hidden;
    transition:
      transform 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      background 0.3s,
      border-color 0.3s;
  }
  .np-track-row:hover {
    transform: translateX(4px);
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.06) 0%, rgba(255, 255, 255, 0.02) 100%);
    border-color: color-mix(in srgb, var(--track-accent) 35%, rgba(255, 255, 255, 0.1));
  }
  .np-track-row:hover .np-track-border { opacity: 1; }
  .np-track-row:hover .np-track-play {
    opacity: 1;
    transform: scale(1);
  }
  .np-track-row:hover .np-track-rank { color: var(--track-accent); }

  .np-track-border {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    padding: 1px;
    background: linear-gradient(90deg, color-mix(in srgb, var(--track-accent) 60%, transparent), transparent 70%);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s;
    pointer-events: none;
  }

  .np-track-rank {
    font-size: 0.65rem;
    font-weight: 800;
    font-variant-numeric: tabular-nums;
    color: var(--sp-text-dim);
    letter-spacing: 0.05em;
    min-width: 1.25rem;
    transition: color 0.3s;
  }

  .np-track-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    background: linear-gradient(145deg, color-mix(in srgb, var(--track-accent) 28%, transparent), rgba(0, 0, 0, 0.2));
    border: 1px solid color-mix(in srgb, var(--track-accent) 35%, transparent);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.08),
      0 0 20px color-mix(in srgb, var(--track-accent) 15%, transparent);
  }

  .np-track-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--track-accent);
    box-shadow: 0 0 14px var(--track-accent);
  }

  .np-track-meta {
    flex: 1;
    min-width: 0;
  }

  .np-track-name {
    font-size: 0.78rem;
    font-weight: 600;
    color: #fff;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .np-track-project {
    font-size: 0.68rem;
    color: var(--sp-text);
  }

  .np-track-plays {
    font-size: 0.68rem;
    color: var(--sp-text-dim);
    flex-shrink: 0;
  }

  .np-track-play {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--sp-green);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    opacity: 0;
    transform: scale(0.85);
    box-shadow: 0 4px 16px rgba(29, 185, 84, 0.35);
    transition: opacity 0.25s, transform 0.25s cubic-bezier(0.22, 1, 0.36, 1);
  }

  @media (prefers-reduced-motion: reduce) {
    .mono-card,
    .np-hero,
    .np-track-row,
    .np-stack-art { transition: none; animation: none; }
    .mono-card:hover,
    .np-hero:hover,
    .np-track-row:hover { transform: none; }
  }

  @media (max-width: 900px) {
    .quick-grid { grid-template-columns: repeat(2, 1fr) !important; }
    .mfy-grid { grid-template-columns: repeat(3, 1fr) !important; }
    .dash-split { grid-template-columns: 1fr !important; }
    .about-grid { grid-template-columns: 1fr !important; }
    .np-hero { min-height: 220px; }
    .np-hero-copy { max-width: 55%; }
    .np-stack-art { width: min(75%, 320px); right: -16px; }
  }

  @media (max-width: 600px) {
    .quick-grid,
    .mfy-grid { grid-template-columns: repeat(2, 1fr) !important; }
    .np-stack-art { width: min(72%, 280px); right: -12px; }
    .np-hero-title { font-size: 1.15rem; }
  }
</style>
